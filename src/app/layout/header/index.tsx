import "./styles.scss";
const Header = () => {
  return (
    <header className={"app-header"}>
      <span className={"app-header-title"}>Stock Prices</span>
    </header>
  );
};

export default Header;
